@model IEnumerable<_19T1021007.DomainModels.Customer>

@{
    ViewBag.Title = "Quản lý khách hàng";
}

<div class="box box-primary">
    <div class="box box-body">
        @*form tìm kiếm*@
        <form action="~/Customer" method="get">
            <div class="input-group">
                <input type="text" name="searchValue" class="form-control" placeholder="Search..." value="@ViewBag.SearchValue">
                <span class="input-group-btn">
                    <button type="submit" name="search" id="search-btn" class="btn btn-info">
                        <i class="fa fa-search"></i>
                    </button>
                    <a href="~/Customer/Create" class="btn btn-primary" style="margin-left:5px;">
                        <i class="fa fa-plus-circle">Bổ sung</i>
                    </a>
                </span>
            </div>
        </form>
        @*danh sách nhà cung cấp, phân trang*@
        <p>Có <strong>@ViewBag.RowCount</strong> khách hàng trong tổng số <strong>@ViewBag.PageCount</strong> trang</p>
        <div class="table-responsive">
            <table class="table table-bordered table-hover table-striped">
                <thead>
                    <tr class="bg-primary">
                        <th>Tên khách hàng</th>
                        <th>Tên liên lạc</th>
                        <th>Địa chỉ</th>
                        <th>Quốc gia</th>
                        <th>Thành phố</th>
                        <th>Mã bưu chính</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @*@for (int i = 0; i < 5; i++)
        {
            <tr>
                <td>Nguyễn Thế Anh</td>
                <td>131 Trần Phú, Huế</td>
                <td>0123456789</td>
                <td>Việt Nam</td>
                <td>Thành Phố Huế</td>
                <td>49000</td>
                <td class="text-right">
                    <a href="~/Customer/Edit" class="btn btn-sm btn-primary" title="Sửa">
                        <i class="fa fa-edit"></i>
                    </a>
                    <a href="~/Customer/Delete" class="btn btn-sm btn-danger" title="Xóa">
                        <i class="fa fa-trash"></i>
                    </a>
                </td>
            </tr>
        }*@
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.CustomerName</td>
                            <td>@item.ContactName</td>
                            <td>@item.Address</td>
                            <td>@item.Country</td>
                            <td>@item.City</td>
                            <td>@item.PostalCode</td>
                            <td class="text-right">
                                <a href="~/Customer/Edit/@item.CustomerID" class="btn btn-sm btn-primary" title="Sửa">
                                    <i class="fa fa-edit"></i>
                                </a>
                                <a href="~/Customer/Delete/@item.CustomerID" class="btn btn-sm btn-danger" title="Xóa">
                                    <i class="fa fa-trash"></i>
                                </a>
                            </td>
                        </tr>
                    }

                </tbody>
            </table>
        </div>
        <div class="text-center">
            <ul class="pagination">
                @for (int i = 1; i <= ViewBag.PageCount; i++)
                {
                    if (i == ViewBag.Page)
                    {
                        <li class="active"><a href="#">@i</a></li>
                    }
                    else
                    {
                        <li><a href="~/Customer?page=@i&searchValue=@ViewBag.SearchValue">@i</a></li>
                    }
                }
                @*<li class="active"><a href="#">1</a></li>
        <li><a href="#">2</a></li>
        <li><a href="#">3</a></li>
        <li><a href="#">4</a></li>
        <li><a href="#">5</a></li>*@
            </ul>
        </div>
    </div>
</div>


